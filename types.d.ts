type Settings = {
  enabled: boolean;
  url: string;
};

type MusicInfo = {
  title: string;
  artist: string;
  album: string;
  year: string;
  image: string | null;
  url: string;
  playing: boolean;
};
